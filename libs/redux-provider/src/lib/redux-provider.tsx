/* eslint-disable-next-line */
export interface ReduxProviderProps {}

export function ReduxProvider(props: ReduxProviderProps) {
  return (
    <div>
      <h1>Welcome to ReduxProvider!</h1>
    </div>
  );
}

export default ReduxProvider;
